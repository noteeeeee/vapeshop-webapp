{
  "name": "@vapeshop-webapp/config",
  "version": "0.1.0",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "dev": "tsc -w --preserveWatchOutput --outDir dist",
    "prepare": "tsc --declaration --outDir dist --sourceMap -p ."
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^17.0.25",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "dotenv": "^14.3.2",
    "env-var": "^7.1.1"
  }
}
